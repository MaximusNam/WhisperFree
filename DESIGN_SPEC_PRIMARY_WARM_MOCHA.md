# Спецификация Основного Дизайн-Макета: «Тёплый Дымчатый Мокко» (Warm Smoky Mocha)

> **Статус**: ✅ **УТВЕРЖДЕН В КАЧЕСТВЕ ОСНОВНОГО ДИЗАЙНА**  
> **Конфигурационный файл**: [`theme_primary.json`](file:///c:/1111CODEX_С/АНАЛОГ%20WISPER/whisperfree/theme_primary.json)  
> **Файл с запасными вариантами**: [`themes_backup.json`](file:///c:/1111CODEX_С/АНАЛОГ%20WISPER/whisperfree/themes_backup.json)

---

## 1. 📸 Выбранный Дизайн-Макет

Макет утвержден пользователем на основе интерактивного тестирования в рабочей среде (светлые формы, CRM, Word, браузер):

```
┌────────────────────────────────────────────────────────┐
│   (●)  Запись…                                         │
│  ════════════════════════════════════════════════════  │  ← полоска уровня звука
└────────────────────────────────────────────────────────┘
```

* **Форм-фактор**: Полная овальная капсула (pill-shape).
* **Размер**: **185 × 36 px** (аккуратный мини-овал, не перекрывает рабочую область).
* **Прозрачность**: **13%** (текст документов под оверлеем полностью читается сквозь стекло).
* **Минимализм**: Убрано время записи и лишние боковые индикаторы; оставлены только точка статуса, надпись и живая полоска звука.

---

## 2. 📐 Геометрия и Расположение Элементов

| Параметр | Значение | Описание |
|---|---|---|
| **Ширина (Width)** | `185 px` | Компактная ширина, в 2 раза меньше стандартной |
| **Высота (Height)** | `36 px` | Аккуратная высота строки |
| **Радиус скругления (Corner Radius)** | `18 px` | Ровно половина высоты = идеальный овал / капсула |
| **Внутренние отступы (Padding)** | `слева: 14 px, справа: 14 px` | Обеспечивает баланс воздуха внутри овала |
| **Толщина контура (Border Width)** | `1.5 px` | Четкая грань, не сливающаяся с белым фоном |
| **Диаметр точки статуса (Dot)** | `8 px` | Расположена на расстоянии 14 px от левого края |
| **Высота полоски звука (Level Bar)** | `2.5 px` | Идёт по нижнему краю плашки с отступами по 14 px с краев |

---

## 3. 🎨 Цветовая Палитра и Эффект Стекла

### Поверхность плашки (Surface):
* **Фоновая заливка (Background)**:
  * RGBA: `rgba(48, 40, 36, 0.13)` *(13% легкого кофейно-графитового тонирования)*
  * HEX (при 100% непрозрачности): `#302824`
* **Окантовка (Border Rim)**:
  * RGBA: `rgba(95, 83, 78, 0.75)` *(цвет темного шоколадного мокко)*
  * HEX: `#5F534E`
* **Верхний световой блик (Inner Specular Rim)**:
  * RGBA: `rgba(255, 255, 255, 0.45)` *(тончайшая верхняя фаска света)*
* **Внешняя тень (Drop Shadow)**:
  * `0 6px 16px rgba(28, 25, 23, 0.12)` *(мягкая естественная глубина)*
* **Размытие (Backdrop Blur)**:
  * `0 px` *(размытие отключено, чтобы мелкий текст документов за стеклом не превращался в кашу)*

---

## 4. ✍️ Типографика

* **Основной шрифт**: `Segoe UI Variable Text` *(нативный шрифт Windows 11)*
* **Резервные шрифты (Fallbacks)**: `Segoe UI`, `Inter`, `Arial`, `sans-serif`
* **Размер шрифта**: `13 px` (`10 pt`)
* **Начертание (Weight)**: `SemiBold` (600)
* **Межбуквенный интервал (Letter Spacing)**: `0.01 em`
* **Цвет текста**: `#1C1917` *(глубокий теплый антрацит, максимальная контрастность к белому фону)*
* **Выравнивание**: По левому краю, `10 px` от точки статуса.

---

## 5. 🚦 Таблица Состояний Оверлея (State Machine)

Программа переключает цвет точки, полоски звука и текст в зависимости от этапа обработки:

| Состояние (`state`) | Отображаемый текст | Цвет точки и полоски | HEX цвет | Назначение |
|---|---|---|---|---|
| **`recording`** | `Запись…` | Тёплый мокко-графит (пульс) | `#78716C` | Клавиша зажата, микрофон слушает речь |
| **`silent`** | `Микрофон молчит` | Приглушенный пепельный | `#A8A29E` | Клавиша нажата, но голос не звучит |
| **`sending`** | `Распознаю…` | Тёплый янтарь | `#D97706` | Аудио отправлено в Groq Whisper API |
| **`refining`** | `Правлю текст…` | Глубокий мокко | `#57534E` | Постобработка и исправление текста |
| **`ok`** | `Готово` | Мягкий оливковый изумруд | `#16A34A` | Текст успешно вставлен в активное поле |
| **`error`** | `Ошибка` | Рубиновый коралл | `#DC2626` | Сбой соединения / ошибка вставки |

---

## 6. 📁 Готовый JSON-файл темы: `theme_primary.json`

Файл спецификации сохранен в папке проекта:  
📁 [**whisperfree/theme_primary.json**](file:///c:/1111CODEX_С/АНАЛОГ%20WISPER/whisperfree/theme_primary.json)

```json
{
  "theme_id": "warm_smoky_mocha",
  "name_ru": "Тёплый Дымчатый Мокко",
  "geometry": {
    "width_px": 185,
    "height_px": 36,
    "corner_radius_px": 18,
    "border_width_px": 1.5,
    "indicator_dot_diameter_px": 8,
    "level_bar_height_px": 2.5
  },
  "surface": {
    "background_color_rgba": "rgba(48, 40, 36, 0.13)",
    "border_color_rgba": "rgba(95, 83, 78, 0.75)",
    "text_color_hex": "#1C1917"
  },
  "states": {
    "recording": { "label": "Запись…", "color": "#78716C" },
    "silent":    { "label": "Микрофон молчит", "color": "#A8A29E" },
    "sending":   { "label": "Распознаю…", "color": "#D97706" },
    "refining":  { "label": "Правлю текст…", "color": "#57534E" },
    "ok":        { "label": "Готово", "color": "#16A34A" },
    "error":     { "label": "Ошибка", "color": "#DC2626" }
  }
}
```

---

## 7. 🗄️ Запасные Варианты Дизайна: `themes_backup.json`

Остальные 6 альтернативных вариантов сохранены в отдельном архиве:  
📁 [**whisperfree/themes_backup.json**](file:///c:/1111CODEX_С/АНАЛОГ%20WISPER/whisperfree/themes_backup.json)

1. **Холодный Сланец (Slate)** — классический серый контур.
2. **Графит + Коралловая точка** — графитовый корпус с алой точкой записи.
3. **Титановый Графит** — графит с белой сатиновой кромкой.
4. **Тёплый Графит + Янтарь** — теплый графит с золотисто-янтарной полоской.
5. **Студийный Минимал 1px** — ультратонкий 1px кант, 9% затемнения.
6. **Аквамарин / Циан** — яркий бирюзовый контур.
7. **Кристальная Линза** — нейтральное прозрачное стекло.

---

## 8. 💻 Как загружать тему в Python-код (`overlay.py`)

```python
import json
from pathlib import Path

def load_primary_theme() -> dict:
    """Загружает основной утвержденный макет 'Тёплый Дымчатый Мокко'."""
    theme_file = Path(__file__).parent / "theme_primary.json"
    with open(theme_file, "r", encoding="utf-8") as f:
        return json.load(f)
```
